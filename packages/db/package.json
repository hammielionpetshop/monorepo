{
  "name": "@petshop/db",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:seed": "tsx src/seed.ts",
    "db:seed-products": "tsx src/seed-products.ts",
    "db:seed-permissions": "tsx src/seed/permissions.ts",
    "db:seed-local": "tsx src/seed/local-fixtures.ts",
    "db:clone-master": "tsx src/seed/clone-master.ts"
  },
  "dependencies": {
    "drizzle-orm": "latest",
    "postgres": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "drizzle-kit": "latest",
    "tsx": "latest",
    "dotenv": "latest",
    "argon2": "0.44.0",
    "@petshop/shared": "workspace:*"
  }
}
